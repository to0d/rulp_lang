(defclass class1 ()
    (defvar ?value 1 public)
)
;=>class1

(defvar ?arr {})
;=>&?arr

(add ?arr (new class1))
;=>{un-0}

(add ?arr (new class1))
;=>{un-0,un-1}

(loop for ?o in (to-list ?arr) do
    (print ?o::?value)
)
;err:
Can't decRef: class1@un-0, ref=-1
at main: (loop for ?o in (to-list ?arr) do (print ?o::?value))
;eof

