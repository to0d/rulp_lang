(defun fun1 (?x ?y)
    (if (<= ?x 0)
        (return 0))
    (return ?y)
)[opt-lco]
;=>

(defun fun2 ()
    (return (+ 1 2))
)
;=>

(print-impl fun1)
;;;

(opt-status)
;=>

(fun1 0 (fun2))
;=>

(runtime-call-count fun2) ; The fun2 should not be called at all
;=>0

(attribute-of fun1)
;=>'(opt-lco)

(print-impl fun1)
;;;

(opt-status)
;=>

(_ginfo)
;;;