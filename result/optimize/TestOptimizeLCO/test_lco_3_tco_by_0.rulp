;; fact(n) = n*fact(n-1)
(defun fact (?v)
    (if (<= ?v -1)(return -1))
    (return (* ?v (fact (- ?v 1))))
)[opt-tco opt-lco]
;=>

(fact 0)
;=>

(attribute-of fact)
;=>

(print-impl fact)
;;;

(runtime-call-count fact) ; should only be called once
;=>1

(opt-status)
;=>

(_ginfo)
;;;