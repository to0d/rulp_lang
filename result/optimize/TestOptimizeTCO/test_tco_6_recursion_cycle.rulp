(defun fun4a (?v) 
    (do 
        (if (<= ?v 0)(return 0))
        (return (+ ?v (fun4b (- ?v 1))))
    )
)[opt-tco]

(defun fun4b (?v) 
    (do 
        (if (<= ?v 0)(return 0))
        (return (+ ?v (fun4a (- ?v 1))))
    )
)[opt-tco]
;=>

(list-function-in-return fun4a)
;=>

(attribute-of fun4a)
;=>

(list-function-in-return fun4b)
;=>

(attribute-of fun4b)
;=>

(fun4a 3)
;=>

(tco-count)
;=>1

(_ginfo)
;=>