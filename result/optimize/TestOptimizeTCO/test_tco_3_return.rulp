;; fact(n) = n + fact(n-1)
(defun fact (?v)
    (do 
        (if (<= ?v 0)(return 0))
        (return (+ ?v (fact (- ?v 1))))     ; <== cps in return expression
    )
)[opt-tco]
;=>

(list-function-in-return fact)
;=>

(attribute-of fact)
;=>

(fact 0)
;=>

(fact 1)
;=>

(fact 2)
;=>

(fact 3)
;=>

(fact 100)
;=>

(fact 1000)
;=>

(tco-status)
;=>

(_ginfo)
;;;

