(defun fun0i ()
    (return 0)
)

(defun fun0f ()
    (return 0f)
)

(defun fun0l ()
    (return 0l)
)

(defun fun0d ()
    (return 0d)
)

(defun funx ()
    (return 100)
)
;=>

(power 0 (funx))
;=>

(runtime-call-count funx)
;=>0

(power 0.0 (funx))
;=>

(runtime-call-count funx)
;=>0

(power 0D (funx))
;=>

(runtime-call-count funx)
;=>0

(power 0L (funx))
;=>

(runtime-call-count funx)
;=>0

(power (fun0i) (funx))
;=>

(runtime-call-count funx)
;=>0

(power (fun0f) (funx))
;=>

(runtime-call-count funx)
;=>0

(power (fun0l) (funx))
;=>

(runtime-call-count funx)
;=>0

(power (fun0d) (funx))
;=>

(runtime-call-count funx)
;=>0
