(reduct
    (do
        (defvar ?x 10)
            
        (loop 
            (setq ?x (/ ?x 3))  ; ?x will never be a negative number
            (if (< ?x 0)
                (return ?x))
            (println ?x)
        )
    )
)
;=>(println ?x)
