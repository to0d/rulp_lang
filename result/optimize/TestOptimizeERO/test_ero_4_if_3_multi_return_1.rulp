(defun fun (?v) 

    (if (<= ?v 0) do
        (return 0)
        (return 0)
    )
        
    (return 1)
    (return 2)

)[opt-ero]
;=>

(attribute-of fun)
;=>'(opt-ero)

(print-impl fun)
;=>

(fun 5)
;=>

(opt-status)
;=>

(_ginfo)
;;;