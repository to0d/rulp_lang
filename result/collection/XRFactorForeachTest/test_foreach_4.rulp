(defun fun ((?str string))
    (defvar ?out {})
    (foreach (?line (str-split-line ?str))
        (add ?out ?line))
    (return (to-list ?out))
)
;=>

(fun "abc")
;=>'("abc")

(fun "abc")
;=>'("abc")

(fun "abc")
;=>'("abc")